# One "owner/repo" per line, optionally followed by a branch to check
# instead of the repo's default branch, e.g. "owner/repo release-1.0".
LLNL/zfp staging
# mrzv/diy  # temporarily disabled
ornladios/ADIOS2
Viskores/viskores
# Kitware/ParaView  # temporarily disabled
Alpine-DAV/ascent
Parallel-NetCDF/PnetCDF
# Kitware/catalyst  # temporarily disabled
visit-dav/visit
HDFGroup/hdf5
